package nl.hauntedmc.serverfeatures.api.feature;

/** Defines how a built-in feature participates in the supported public architecture. */
public enum FeatureClassification {
    CAPABILITY_PROVIDER,
    EXTENSION_PROVIDER,
    CAPABILITY_CONSUMER,
    INTERNAL
}
