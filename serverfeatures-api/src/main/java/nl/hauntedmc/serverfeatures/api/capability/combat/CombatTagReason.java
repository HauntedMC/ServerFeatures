package nl.hauntedmc.serverfeatures.api.capability.combat;

/** Stable interaction category represented by an active combat tag. */
public enum CombatTagReason {
    MELEE,
    PROJECTILE,
    PET,
    FISHING_HOOK,
    TNT,
    AREA_EFFECT,
    FIREWORK,
    EVOKER_FANGS,
    EXPLOSION,
    INDIRECT,
    EXTERNAL
}
